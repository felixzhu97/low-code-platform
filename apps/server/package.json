{
  "name": "@lowcode-platform/server",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start:dev": "uvicorn app.main:app --reload --port 8000",
    "start:prod": "uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "build": "echo \"FastAPI server does not require build\"",
    "lint": "echo \"No lint configured for FastAPI server\"",
    "test": "echo \"No tests configured for FastAPI server\""
  }
}
