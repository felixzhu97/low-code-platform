/**
 * Setup Jest test environment
 * Add this to your Jest setupFilesAfterEnv array in jest.config.ts
 */
export function setupJest() {
  // Add any global Jest setup here
  // For example, setup @testing-library/jest-dom matchers if using jsdom
}

// Export for manual setup if needed
export default setupJest;
