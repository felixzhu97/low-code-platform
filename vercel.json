{
  "framework": "nextjs",
  "buildCommand": "bash scripts/build-wasm.sh && yarn workspace @lowcode-platform/web build",
  "installCommand": "yarn install --frozen-lockfile",
  "outputDirectory": "apps/web/.next"
}
