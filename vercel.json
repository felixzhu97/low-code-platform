{
  "framework": "nextjs",
  "buildCommand": "pnpm --filter @lowcode-platform/web build",
  "installCommand": "corepack enable && corepack prepare pnpm@8.15.0 --activate && pnpm install --frozen-lockfile"
}
