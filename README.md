# 低代码平台 (Low-Code Platform)

一个基于 Next.js 和 React 构建的可视化低代码开发平台，支持拖拽式页面构建、组件管理、主题定制和代码导出等功能。

## ✨ 功能特性

### 🎨 可视化编辑

- **拖拽式画布**: 直观的拖拽操作，快速构建页面布局
- **实时预览**: 即时查看设计效果，支持响应式预览
- **组件树视图**: 层级化管理页面组件结构
- **属性面板**: 丰富的属性配置选项

### 🧩 组件系统

- **内置组件库**: 包含按钮、表单、图表等常用组件
- **自定义组件**: 支持创建和管理自定义组件
- **组件分组**: 灵活的组件分类和组织管理
- **组件导入导出**: 跨项目共享组件资源

### 📊 数据与图表

- **数据面板**: 统一管理数据源和数据绑定
- **图表组件**: 支持柱状图、折线图、饼图等多种图表类型
- **表单构建器**: 快速创建表单和数据收集页面

### 🎭 主题与动画

- **主题编辑器**: 可视化定制界面主题和样式
- **动画编辑器**: 为组件添加动画效果
- **响应式设计**: 适配不同屏幕尺寸

### 🚀 协作与导出

- **实时协作**: 多人协同编辑功能
- **代码导出**: 导出为可部署的前端代码
- **模板库**: 预置页面模板，快速开始项目

### ⚡ 性能优化

- **Rust/WASM 加速**: 使用 Rust 编译为 WebAssembly，优化性能关键路径
  - **数据解析**: 高性能 CSV/XML 解析和 JSON 验证
  - **Schema 处理**: 优化的 Schema 序列化/反序列化、验证和迁移
  - **数据映射**: 高效的数据映射生成和应用
  - **布局计算**: 快速的响应式布局计算和碰撞检测
- **优雅降级**: WASM 失败时自动降级到 JavaScript 实现
- **异步处理**: 所有性能关键操作均为异步，不阻塞 UI

## 🛠 技术栈

### 前端

- **框架**: Next.js 15.2.8 + React 19
- **语言**: TypeScript
- **样式**: Tailwind CSS + CSS 变量主题系统
- **UI 组件库**: Radix UI
- **拖拽**: React DnD
- **图表**: Recharts
- **表单**: React Hook Form + Zod 验证
- **状态管理**: React Hooks + 历史记录系统
- **性能优化**: Rust + WebAssembly
  - **Rust**: 系统编程语言，用于性能关键路径
  - **WebAssembly**: 高性能运行时
  - **wasm-bindgen**: Rust-JavaScript 互操作
  - **wasm-pack**: WASM 打包工具

### 后端

- **框架**: NestJS 11
- **语言**: TypeScript
- **架构**: 整洁架构 (Clean Architecture)

### Monorepo

- **包管理**: pnpm workspaces
- **项目结构**: Monorepo 架构

## 📦 项目结构

本项目采用 Monorepo 架构，使用 pnpm workspaces 管理：

```
low-code-platform/
├── apps/
│   ├── web/                    # Next.js 前端应用
│   │   ├── src/
│   │   │   ├── app/            # Next.js App Router
│   │   │   ├── domain/          # 领域层
│   │   │   ├── application/    # 应用层
│   │   │   ├── infrastructure/ # 基础设施层
│   │   │   ├── presentation/   # 表现层
│   │   │   └── shared/          # 共享模块
│   │   ├── public/             # 静态资源
│   │   └── package.json
│   └── server/                 # NestJS 服务端应用
│       ├── src/
│       │   └── main.ts         # 应用入口
│       └── package.json
├── packages/                   # 共享包
│   └── wasm/                   # Rust/WASM 性能优化模块
│       ├── src/                # Rust 源代码
│       │   ├── data_parser.rs  # 数据解析模块
│       │   ├── schema_processor.rs # Schema 处理模块
│       │   ├── data_mapper.rs  # 数据映射模块
│       │   ├── layout_calculator.rs # 布局计算模块
│       │   └── utils.rs        # 工具函数
│       ├── Cargo.toml         # Rust 项目配置
│       └── pkg/                # 编译后的 WASM 包
├── src/                        # 共享代码（当前保留在根目录）
│   ├── domain/                 # 领域层
│   ├── application/            # 应用层
│   ├── infrastructure/         # 基础设施层
│   ├── presentation/           # 表现层
│   └── shared/                 # 共享模块
├── pnpm-workspace.yaml         # pnpm workspace 配置
├── package.json                # 根 package.json
└── tsconfig.json               # 根 TypeScript 配置
```

## 🚀 快速开始

### 环境要求

- Node.js >= 18.0.0
- pnpm >= 8.0.0

### 安装依赖

```bash
# 安装所有依赖（包括前端和后端）
pnpm install

# 构建 WASM 模块（首次运行或 WASM 代码更新后）
pnpm build:wasm
```

### 启动开发服务器

```bash
# 同时启动前端和后端
pnpm dev

# 或分别启动
pnpm dev:web      # 仅启动前端 (http://localhost:3000)
pnpm dev:server   # 仅启动后端 (http://localhost:8000)
```

### 构建生产版本

```bash
# 构建所有应用（包括 WASM）
pnpm build

# 或分别构建
pnpm build:wasm    # 构建 WASM 模块
pnpm build:web     # 构建前端
pnpm build:server  # 构建后端
```

**注意**: 如果修改了 Rust/WASM 代码，需要先运行 `pnpm build:wasm` 重新编译 WASM 模块。

### 启动生产服务器

```bash
# 同时启动前端和后端
pnpm start

# 或分别启动
pnpm start:web     # 启动前端生产服务器
pnpm start:server  # 启动后端生产服务器
```

### 其他命令

```bash
# 代码检查
pnpm lint

# 运行测试
pnpm test          # 前端测试
pnpm test:server   # 后端测试
```

## 🎯 使用指南

### 1. 基础操作

1. **添加组件**: 从左侧组件面板拖拽组件到画布
2. **选择组件**: 点击画布中的组件进行选择
3. **配置属性**: 在右侧属性面板修改组件属性
4. **预览效果**: 点击预览按钮查看最终效果

### 2. 高级功能

- **使用模板**: 从模板库选择预制模板快速开始
- **自定义主题**: 通过主题编辑器定制界面风格
- **添加动画**: 使用动画编辑器为组件添加动效
- **数据绑定**: 在数据面板管理数据源和绑定关系
- **导出代码**: 将设计转换为可部署的前端代码

### 3. 协作开发

- 支持多人实时协作编辑
- 版本历史记录和回滚功能
- 组件库共享和管理

## 🔧 开发

### 架构说明

本项目采用**整洁架构（Clean Architecture）**设计：

- **领域层 (Domain)**: 核心业务逻辑，不依赖任何外部框架
- **应用层 (Application)**: 应用用例和业务流程
- **基础设施层 (Infrastructure)**: 技术实现（持久化、状态管理、WASM 适配器等）
- **表现层 (Presentation)**: UI 组件和用户交互

#### Rust/WASM 性能优化架构

WASM 模块作为基础设施层的实现，采用 **Port-Adapter 模式**：

- **端口 (Port)**: 在应用层定义接口（`IWasmPort`）
- **适配器 (Adapter)**: 在基础设施层实现（`WasmAdapter`）
- **WASM 模块**: Rust 编译的 WebAssembly 代码

这种设计确保了：

- ✅ 整洁架构原则：WASM 作为基础设施实现，不污染领域层
- ✅ 优雅降级：WASM 失败时自动降级到 JavaScript 实现
- ✅ 类型安全：TypeScript 接口保证类型安全
- ✅ 异步处理：所有 WASM 调用均为异步，不阻塞 UI

### 添加新组件

1. 在 `apps/web/src/presentation/components/ui/` 目录下创建新组件
2. 在组件面板中注册组件
3. 在 `apps/web/src/domain/entities/types.ts` 中定义相关类型

### 添加 API 端点

1. 在 `apps/server/src/` 下创建相应的控制器和服务
2. 遵循 NestJS 的模块化架构
3. 使用共享的领域层和应用层代码

### 自定义主题

主题配置在 `apps/web/src/domain/entities/types.ts` 的 `ThemeConfig` 接口中定义，可以通过主题编辑器进行可视化配置。

### 扩展功能

- **数据源**: 在数据面板中添加新的数据连接方式
- **图表类型**: 在 `apps/web/src/presentation/components/charts/` 目录下添加新的图表组件
- **导出格式**: 在代码导出功能中添加新的导出格式

### 开发 WASM 模块

#### 环境要求

- **Rust**: >= 1.70.0
- **wasm-pack**: >= 0.12.0

```bash
# 安装 Rust（如果未安装）
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# 安装 wasm-pack
curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh

# 添加 wasm32 目标
rustup target add wasm32-unknown-unknown
```

#### 开发流程

1. **修改 Rust 代码**: 在 `packages/wasm/src/` 目录下修改 Rust 代码
2. **编译 WASM**: 运行 `pnpm build:wasm` 编译 WASM 模块
3. **测试**: 在浏览器中测试 WASM 功能
4. **查看日志**: WASM 函数调用日志会在控制台显示（使用 `console.warn`）

#### WASM 模块结构

```
packages/wasm/src/
├── data_parser.rs      # 数据解析：CSV/XML 解析、JSON 验证
├── schema_processor.rs # Schema 处理：序列化/反序列化、验证、迁移
├── data_mapper.rs      # 数据映射：映射生成、应用、转换
├── layout_calculator.rs # 布局计算：布局计算、网格对齐、碰撞检测
├── utils.rs            # 工具函数：日志、错误处理
└── lib.rs              # 模块入口：导出所有公共函数
```

#### 添加新的 WASM 功能

1. 在 `packages/wasm/src/` 下创建新的 Rust 模块
2. 在 `lib.rs` 中导入并导出模块
3. 在 `apps/web/src/application/ports/wasm.port.ts` 中定义端口接口
4. 在 `apps/web/src/infrastructure/wasm/wasm.adapter.ts` 中实现适配器
5. 在应用服务中使用 WASM 适配器
6. 运行 `pnpm build:wasm` 重新编译

## 📝 待办事项

- [x] Rust/WASM 性能优化
- [ ] 增加更多图表类型支持
- [ ] 实现数据库连接功能
- [ ] 添加移动端组件库
- [ ] 支持自定义 CSS 样式编辑
- [ ] 实现项目管理和版本控制
- [ ] 添加更多页面模板
- [ ] 完善后端 API 功能
- [ ] 优化 WASM 模块体积
- [ ] 添加 WASM 性能监控

## 🤝 贡献

欢迎提交 Issue 和 Pull Request 来改进项目。

## 📄 许可证

[MIT License](LICENSE)

## 🔗 相关链接

### 前端框架

- [Next.js 文档](https://nextjs.org/docs)
- [React 文档](https://react.dev)
- [NestJS 文档](https://docs.nestjs.com)
- [Tailwind CSS](https://tailwindcss.com)
- [Radix UI](https://www.radix-ui.com)

### 工具与构建

- [pnpm 文档](https://pnpm.io)
- [Rust 文档](https://www.rust-lang.org/learn)
- [WebAssembly](https://webassembly.org/)
- [wasm-bindgen](https://rustwasm.github.io/wasm-bindgen/)
- [wasm-pack](https://rustwasm.github.io/wasm-pack/)

### 架构文档

- [架构文档](docs/README.md) - 完整的架构说明和设计文档
- [TOGAF 架构](docs/architecture/togaf/README.md) - 企业架构视图
- [C4 模型](docs/architecture/README.md) - 软件架构视图
