# 架构对比：当前 vs 优化后

## 📊 当前架构问题可视化

```
┌─────────────────────────────────────────────────────────┐
│                    Presentation                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │  Components  │  │    Hooks     │  │   Styles     │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
│         │                │                  │          │
│         └────────────────┼─────────────────┘         │
│                           │                            │
│                           ▼                            │
│                  ┌──────────────┐                     │
│                  │ Shared/Stores │  ❌ 违反依赖规则    │
│                  └──────────────┘                     │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│                   Application                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │   Services   │  │  Use Cases   │  │     DTO      │ │
│  │              │  │   (空目录)   │  │              │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
│         │                │                  │          │
│         └────────────────┼─────────────────┘         │
│                           │                            │
│                           ▼                            │
│                  ┌──────────────┐                     │
│                  │    Domain    │                     │
│                  └──────────────┘                     │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│                Infrastructure                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │  Persistence │  │State Mgmt    │  │ Data Sources │ │
│  │              │  │  (重复)      │  │              │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
│         │                │                  │          │
│         └────────────────┼─────────────────┘         │
│                           │                            │
│                           ▼                            │
│                  ┌──────────────┐                     │
│                  │ Shared/Stores│  ❌ 状态管理重复     │
│                  └──────────────┘                     │
└─────────────────────────────────────────────────────────┘

问题：
1. ❌ Presentation 直接依赖 Shared/Stores（违反依赖规则）
2. ❌ 状态管理在三个地方（Shared、Application、Infrastructure）
3. ❌ Use Cases 目录存在但为空
4. ❌ Domain/Repositories 为空（缺少接口定义）
5. ❌ 职责不清：Services 在 Application 和 Domain 都有
```

## ✅ 优化后的架构

```
┌─────────────────────────────────────────────────────────────┐
│                      Presentation                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │  Components  │  │   Adapters   │  │   Providers  │     │
│  │  (按模块)    │  │  (调用用例)  │  │              │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│         │                │                  │              │
│         └────────────────┼─────────────────┘              │
│                         │                                 │
│                         ▼                                 │
│                  ┌──────────────┐                        │
│                  │  Use Cases   │  ✅ 通过适配器调用      │
│                  └──────────────┘                        │
└─────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                      Application                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │  Use Cases   │  │     DTO      │  │    Ports     │     │
│  │  (具体用例)  │  │              │  │  (接口定义)  │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│         │                │                  │              │
│         └────────────────┼─────────────────┘              │
│                         │                                 │
│                         ▼                                 │
│                  ┌──────────────┐                        │
│                  │    Domain    │  ✅ 只依赖领域层        │
│                  └──────────────┘                        │
└─────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                      Domain                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │  Entities    │  │ Repositories │  │  Services    │     │
│  │              │  │  (接口定义)   │  │  (领域服务)  │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│         │                │                  │              │
│         └────────────────┼─────────────────┘              │
│                         │                                 │
│                         ▲                                 │
│                         │                                 │
└─────────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                   Infrastructure                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │  Persistence │  │State Mgmt    │  │ Data Sources │     │
│  │  (实现仓储)  │  │  (统一实现)  │  │  (统一实现)  │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│         │                │                  │              │
│         └────────────────┼─────────────────┘              │
│                         │                                 │
│                         ▲                                 │
│                         │                                 │
│                  ┌──────────────┐                        │
│                  │    Ports     │  ✅ 实现所有接口        │
│                  └──────────────┘                        │
└─────────────────────────────────────────────────────────────┘

优势：
1. ✅ 清晰的依赖方向（从外到内）
2. ✅ 状态管理统一在 Infrastructure
3. ✅ Use Cases 具体实现业务用例
4. ✅ Domain 定义所有接口
5. ✅ 职责清晰：Domain 业务逻辑，Application 用例编排，Infrastructure 实现
```

## 🔄 依赖关系对比

### 当前（有问题）

```
Presentation
    ↓ (直接依赖)
Shared/Stores  ← Application ← Infrastructure
    ↑                              ↓
    └──────────────────────────────┘
         ❌ 循环依赖风险
```

### 优化后（正确）

```
Presentation
    ↓ (通过适配器)
Application (Use Cases)
    ↓ (依赖接口)
Domain (Entities, Repositories Interfaces)
    ↑ (实现接口)
Infrastructure (Repository Implementations)
```

## 📁 目录迁移映射

| 当前位置                                               | 优化后位置                                                        | 说明                     |
| ------------------------------------------------------ | ----------------------------------------------------------------- | ------------------------ |
| `shared/stores/*.store.ts`                             | `infrastructure/state-management/zustand/*.store.impl.ts`         | 状态管理统一到基础设施层 |
| `application/services/*.service.ts`                    | `application/use-cases/**/*.use-case.ts`                          | 服务重构为用例           |
| `application/services/component-management.service.ts` | `application/use-cases/component/create-component.use-case.ts` 等 | 拆分为多个用例           |
| `domain/entities/types.ts`                             | `domain/entities/*.entity.ts`                                     | 拆分为多个实体文件       |
| `domain/services/component-factory.service.ts`         | `domain/services/component-factory.service.ts`                    | 保持不变（领域服务）     |
| `infrastructure/persistence/local-storage/`            | `infrastructure/persistence/local-storage/*.repository.impl.ts`   | 实现仓储接口             |
| `presentation/components/`                             | `presentation/components/` (重组)                                 | 按模块重组               |
| `presentation/hooks/`                                  | `presentation/hooks/`                                             | 保持不变                 |
| -                                                      | `presentation/adapters/`                                          | 新增适配器层             |
| -                                                      | `domain/repositories/*.repository.ts`                             | 新增仓储接口             |
| -                                                      | `application/ports/`                                              | 新增端口定义             |

## 🎯 关键改进点

### 1. 状态管理统一

**当前**：三个地方都有状态管理

- `shared/stores/`
- `application/stores/`
- `infrastructure/state-management/stores/`

**优化后**：统一在基础设施层

- `infrastructure/state-management/zustand/`

### 2. 用例实现

**当前**：`application/use-cases/` 目录存在但为空

**优化后**：每个用例一个文件

- `application/use-cases/component/create-component.use-case.ts`
- `application/use-cases/component/update-component.use-case.ts`
- 等等

### 3. 仓储模式

**当前**：`domain/repositories/` 为空

**优化后**：

- `domain/repositories/` 定义接口
- `infrastructure/persistence/` 实现接口

### 4. 适配器模式

**当前**：Presentation 直接调用服务

**优化后**：

- `presentation/adapters/` 作为中间层
- 适配器调用用例，转换数据格式

## 📝 实施优先级

### 🔴 高优先级（立即处理）

1. 统一状态管理到 `infrastructure/state-management`
2. 定义 `domain/repositories` 接口
3. 实现 `infrastructure/persistence` 仓储

### 🟡 中优先级（近期处理）

1. 将 `application/services` 重构为用例
2. 创建 `presentation/adapters`
3. 拆分 `domain/entities/types.ts`

### 🟢 低优先级（长期优化）

1. 重组 `presentation/components`
2. 提取值对象到 `domain/value-objects`
3. 完善 DTO 和 Mapper
